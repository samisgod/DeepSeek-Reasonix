<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Hardened-runtime entitlements required to notarize the Electron
	     (Chromium + Go service) app. V8's JavaScript engine needs JIT and
	     writable/executable memory; Electron ships frameworks and helper
	     binaries that aren't signed with our team ID, so library validation
	     must accept them. Kept minimal — no network/file entitlements, since
	     this is a hardened-runtime carve-out, not an App Sandbox profile. -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
