node_modules
# Build output is generated by `pnpm build`; keep only the placeholder so the
# Go `//go:embed all:frontend/dist` stays compilable on a fresh checkout before
# the first build.
dist/*
!dist/.gitkeep

*.local

# This project uses pnpm; reject the npm lockfile.
package-lock.json

# Playwright browsers for the real-DOM bench (kept inside the repo; see bench/).
.pw-browsers
bench/results.json
bench/app-memory-results.json
bench/app-memory-artifacts/
